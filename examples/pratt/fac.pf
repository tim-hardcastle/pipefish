~~ A file that just contains a factorial function, so the various scripts can all share it.

def 

fac(i int) -> int :
    i < 0 :
        error "can't take the factorial of a negative number"
    i == 0 :
        1
    else : 
        i * fac i-1
