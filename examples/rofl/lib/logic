import lib/sugar

// We define the boolean type.
|bool| -> [TF]
<(bool)> -> $1

// And its operations.
F and (bool) -> F
T and (bool) -> $1
F or (bool) -> $1
T or (bool) -> T
not T -> F
not F -> T

// And a conditional.
if T then (.*?) else (.*) -> $1
if F then (.*?) else (.*) -> $2


