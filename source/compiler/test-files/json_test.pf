import

NULL::"encoding/json"

newtype

Person = struct(name string, age int?)

make list{Person}

const

FOO = `"foo"`
MAP = `{"a": 1, "b": 2}`
JOHN = `{"name": "John", "age": 22}`
FRED = `{"name": "Fred", "age": null}`
PEOPLE = `[{"name": "John", "age": 22}, {"name": "Fred", "age": null}]`
