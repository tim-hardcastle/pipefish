newtype

Person = struct(name string, age int?)
Cat = struct(name string, age int)
Dog = struct(name string, friends list)

var

john = Person("John", 22)
rex = Dog("Rex", ["Fido", "Butch"])
myList = ["a", "b", "c", "d"]
myOtherList = ["a", "b", ["x", "y", "z"], "d"]
thirdList = [john, rex]
myMap = map("a"::1, "b"::2, "c"::3, "d"::4)
otherMap = map("a"::[0, 1], "b"::[2, 3])

diffMap ... = "a"::99, "b"::99
diffList ... = 0::"x", 1::"y"

F = func(x) : x 

badType = Addable
intType = int 
badNum = 99 
badVal any = true
badString = "foo"

badFieldsA ... = name::"John"
badFieldsB ... = name::"John", age::true
badList = []

def 

xwithy(x, y) :
    x with y

