newtype

thing = clone list

var 

ks = [1, 2, 3, 4]
double = func(x) : 2 * x

const

MP = map(1::"fee", 2::"fie", 3::"fo", 4::"fum")

def 

foo(x) :
    x >> MP[that]

goo(x list/int) :
    x >> MP[that]

boo(x, y) :
    x >> y

